version: '3'
services:
  api:
    build:
      context: .
      dockerfile: ./infra/dev/.
    ports:
      - '8080:8080'
    working_dir: /opt/app
    volumes:
      - ./cmd:/opt/app/cmd
      - ./gen:/opt/app/gen
      - ./internal:/opt/app/internal
      - ./swagger-ui:/opt/app/swagger-ui
      - ./go.mod:/opt/app/go.mod
      - ./go.sum:/opt/app/go.sum
